<!--
  assets.qrc —— 编进 exe 的只读数据。

  收进来的判据只有一条：**UI 工作过程中不会被写**。
  会被写的东西一律留在磁盘上，因为编进 exe 就改不了了：
      多国语言表   跟着工程走（<工程>/i18n_*.xls），要改文案
      自定义控件   「保存成控件」往 <工具目录>/assets/widgets.d/ 里写
      画布背景图   <工具目录>/assets/canvas/，是给人往里放 jpg 的口子

  这里的每一项在磁盘上都能被同名文件盖掉（见 src/core/AssetPaths.h）：
  外部有就用外部的，没有才用这份内置的。所以要定制照样能把文件丢出来改，
  不定制就一个都不用摆在目录里。

  【新建工程的骨架没有收进来】它只读，本来够格；但骨架里的文件名是中文，
  而 CMake/ninja 处理不了 qrc 里的非 ASCII 路径 —— 依赖会被写成 "??????.bat"，
  构建直接失败（和 build.bat 抬头记的 moc 那个是同一类问题）。绕开要在
  resources/ 里另放一份 ASCII 名的副本，那就有两份会漂移的源头，不划算。
  所以 template/ 仍然是 <工具目录> 下的一个真目录。
-->
<RCC>
    <qresource prefix="/assets">
        <file alias="widgets.json">assets/widgets.json</file>
        <file alias="typecodes.ini">assets/typecodes.ini</file>
        <file alias="widgets.d/slider.json">assets/widgets.d/slider.json</file>
        <file alias="widgets.d/vslider.json">assets/widgets.d/vslider.json</file>
    </qresource>
</RCC>
